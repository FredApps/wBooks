<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET is needed for the Gutenberg OPDS browser to fetch catalogue / books,
         and (when configured) for Sentry to upload crash events. Wear Data Layer
         transports locally over BT/Wi-Fi and does not require this permission. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CompanionApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!--
          Sentry config. DSN comes from local.properties via manifestPlaceholders
          (empty = SDK no-ops). auto-init is disabled so CompanionApp.onCreate can
          gate init on the user's "Crash reports" opt-out (mirrored from the watch).
        -->
        <meta-data android:name="io.sentry.dsn" android:value="${sentryDsn}" />
        <meta-data android:name="io.sentry.auto-init" android:value="false" />
        <meta-data android:name="io.sentry.environment" android:value="phone" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
