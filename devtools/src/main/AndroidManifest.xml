<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_devtools"
        android:roundIcon="@drawable/ic_devtools"
        android:theme="@android:style/Theme.DeviceDefault">

        <!--
            Launcher activity. NoDisplay theme so it never paints — it just fires
            the Developer options intent and finishes. On Galaxy Watch 6, there's
            no third-party-accessible deep link to the Wireless debugging toggle
            specifically; this lands on the Developer options page where Wireless
            debugging is one tap away.
        -->
        <activity
            android:name=".LaunchDevOptionsActivity"
            android:label="@string/launcher_label"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Tile carousel shortcut. The user adds this manually via long-press on
            an existing tile → Add → DevTools (Wear OS doesn't allow third-party
            apps to programmatically place tiles in the carousel).
        -->
        <service
            android:name=".DevOptionsTileService"
            android:label="@string/tile_label"
            android:icon="@drawable/ic_devtools"
            android:exported="true"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_preview" />
        </service>

    </application>
</manifest>
